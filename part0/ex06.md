When submitting a new note, there is just one POST request when working with the SPA. The JS code from the server-side is what dictates the behaviour of the note immediately appearing to the user at the bottom.